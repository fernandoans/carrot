spring:
  application:
    name: carrot-back
  datasource:
    url: jdbc:h2:file:./data/carrotdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console
